<Window x:Class="fitnessTracker24_25.AddEjercicioWindows"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:fitnessTracker24_25"
        mc:Ignorable="d"
        Title="Añadir ejercicios" 
        Height="200" 
        Width="400"
        WindowStartupLocation="CenterOwner">
    
    <Grid Name="AddWindowGrid">
        <!-- Definimos las filas y columnas -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        
        <!-- Etiquetas y cuadros de texto -->
        <Label Content="Nombre:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
        <TextBox Name="NameTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <Label Content="Descripción:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
        <TextBox Name="DescriptionTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <Label Content="Grupo Muscular:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
        <TextBox Name="GroupTextBox" Grid.Row="2" Grid.Column="1" Margin="5"/>

        
        <!-- Botones -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="5">
            <Button Content="Aceptar" Width="75" Margin="5" Click="AcceptButton_Click"/>
            <Button Content="Cancelar" Width="75" Margin="5" Click="CancelButton_Click"/>
        </StackPanel>

    </Grid>
</Window>
